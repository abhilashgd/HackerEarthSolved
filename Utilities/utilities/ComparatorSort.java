package utilities;
/*
 * 
import java.util.Collections;
import java.util.Comparator;

	 Collections.sort(points,new Comparator<Point>() {

			 public int compare(Point o1, Point o2) {
				 int result = Integer.compare((int) o1.getX(), (int) o2.getX());
		            if (result == 0 ) {
		                result = Integer.compare((int) o1.getY(), (int) o2.getY());
		            }
		            return result;
			 }
			 });
			 
			 
			 	  static class Point{
		    private int x;
		    private int y;

		    int getX() {
		        return x;
		    }

		    void setX(int x) {
		        this.x = x;
		    }

		    int getY() {
		        return y;
		    }

		    void setY(int y) {
		        this.y = y;
		    }

		    Point(int x, int y) {
		        this.x = x;
		        this.y = y;
		    }

		    Point() {
		    }
		}
*	 
*			 
*			 
*/

public class ComparatorSort {

}
